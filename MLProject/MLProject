name: heart_disease_ci

python_env: conda.yaml

entry_points:
  main:
    parameters:
      dataset: {type: str, default: "heartdisease_preprocessing.csv"}
    command: "python modelling.py --dataset {dataset}"
